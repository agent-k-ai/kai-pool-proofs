{
  "name": "kai-pool-proofs",
  "version": "0.1.0-node.20260913a",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Own-RPC SP1 VOLUME proof node and separate receipt diagnostics for chain 46630.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r build && vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
