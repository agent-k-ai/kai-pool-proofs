/**
 * @kai-pool-proofs/cli
 *
 * Public VOLUME proof CLI: inspect, fetch, verify, plan, submit, confirm,
 * rewards, claim. Own RPC and own wallet only; no operator fallback.
 *
 * C0 checkpoint: package scaffold only. Command implementation follows the
 * agreed wire schema.
 *
 * Apache-2.0. Copyright 2026 Alpha Tech Organization.
 */
export const CLI_NAME = "volume-proof" as const;
export const CLI_VERSION = "0.1.0" as const;